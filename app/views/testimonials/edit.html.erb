<h1>Editar Depoimento</h1>
<p>Não insira nomes ou qualquer outro tipo de informação pessoal.</p>
<%= bootstrap_form_for(@testimonial) do |t| %>
	<%= t.text_field :title, label: "Título", required: true %>
	<%= t.text_area :body, label: "Texto", size: "30x10", required: true, maxlength: 2000  %>
	<%= t.submit "Enviar" %>
	<%= link_to "Cancelar", :back, {:class=>"btn btn-default"} %>
	<% end %>