<div class="row">
    <div class="col-md-12" style="text-align: center">
         <h3>Gráfico das dorgas</h3>
    </div>
  <div class="col-md-6">text away
  </div>
  <div class="col-md-6">
      <!--Div that will hold the pie chart-->
    <div id="chart_div"></div>

  </div>

</div>

<% drugs.each do |drug| %>
    <%= hidden_field_tag "drugs[]", "#{drug.name}|#{drug.usage_percentage}" %>
<% end %>